import { useEffect } from 'react';

export const RybbitAnalytics = () => {
  useEffect(() => {
    // The script is already loaded via index.html
    // You can add any additional initialization here if needed
  }, []);

  return null;
};